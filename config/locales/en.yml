en:
  field_is_computed: Computable
  field_formula: Formula

  label_available_custom_fields: Available custom field

  text_supported_formulas: 'The formula min(), max(), sum(), product(), division(), 
    custom() are supported. Examples: sum(cfs[1], cfs[2]), custom(cfs[1] + cfs[2] * cfs[3])'
  text_double_click_on_available_field: Double click on the desired field to 
    transer it to the formula field.
  
  error_unknown_formula: Unknown formula
  error_unsupported_formula: Unsupported formula
  error_missing_fields: Fields are required
  error_missing_arguments: Invalid operators or delimiters
  error_invalid_arguments: Invalid formula syntax
  error_unsupported_field_format: Invalid format for at least one field
  error_invalid_field: Invalid field
